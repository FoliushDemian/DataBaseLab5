#spring:
#  datasource:
#    url: jdbc:mysql://localhost/database5?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
#    username: root
#    password: root
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  jpa:
#    # Show or not log for each sql query
#    show-sql: true
#    hibernate:
#      # Hibernate ddl auto (validate, create, create-drop, update)
#      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
#
#  mvc:  # the fix for swagger
#    pathmatch:
#      matching-strategy: ant_path_matcher
#
#server:
#  port: 8080

spring:
  datasource:
    url: jdbc:mysql://google/database5?cloudSqlInstance=lab1-402020:europe-central2:database5&socketFactory=com.google.cloud.sql.mysql.SocketFactory
    username: database5
    password: root
  jpa:
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        default_schema: public