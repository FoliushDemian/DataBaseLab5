spring:
  datasource:
    url: jdbc:mysql://google/database6?cloudSqlInstance=clouddatabase-402217:europe-west1:database5&socketFactory=com.google.cloud.sql.mysql.SocketFactory
    username: demian
    password: lox
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        default_schema: public
